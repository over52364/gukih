<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Поздравление для Лены</title>
    <style>
        body {
            text-align: center;
            background-color: #ffe6f2;
            font-family: Arial, sans-serif;
            overflow: hidden;
        }
        h1 {
            color: #d63384;
            font-size: 36px;
            margin-top: 20vh;
        }
        p {
            font-size: 24px;
            color: #ff1493;
        }
        .flower {
            position: absolute;
            width: 50px;
            height: 50px;
            background-image: url('https://pngimg.com/uploads/flower/flower_PNG5478.png');
            background-size: cover;
            opacity: 0.8;
            animation: fall 5s linear infinite;
        }
        @keyframes fall {
            from {
                transform: translateY(-100px);
                opacity: 1;
            }
            to {
                transform: translateY(100vh);
                opacity: 0;
            }
        }
    </style>
</head>
<body>
    <h1>С 8 Марта, Лена! 💐</h1>
    <p>Желаю счастья, любви, вдохновения и исполнения всех желаний!</p>
    
    <script>
        function createFlower() {
            const flower = document.createElement('div');
            flower.classList.add('flower');
            document.body.appendChild(flower);

            let startPosition = Math.random() * window.innerWidth;
            let delay = Math.random() * 5;

            flower.style.left = startPosition + 'px';
            flower.style.animationDelay = delay + 's';

            setTimeout(() => {
                flower.remove();
            }, 5000);
        }

        setInterval(createFlower, 500);
    </script>
</body>
</html>
